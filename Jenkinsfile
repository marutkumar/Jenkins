pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Your build steps here
               echo 'mvn clean install'
            }
        }
        
        // Add more stages as needed
    }
}
